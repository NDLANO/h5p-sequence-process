import React from 'react';

const Placeholder = ({
    children
                     }) => {

    return (
        <div
            className="h5p-droparea"
        >
            {children}
        </div>
    );
};

export default Placeholder;
