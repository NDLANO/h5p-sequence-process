import React from 'react';

const ActionsList = (
    {children}
) => {
    return (
        <div className={"h5p-sequence-actionlist"}>
            {children}
        </div>
    );
};

export default ActionsList;